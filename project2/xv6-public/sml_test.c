#include "types.h"
#include "stat.h"
#include "user.h"

int main(int argc, char *argv[])
{ 
    // char *args[1] = {0}; 
    // setmemorylimit(getpid(),12289);
    // exec("stressfs",args);

    procdump();
    exit();
}